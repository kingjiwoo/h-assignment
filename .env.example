# 둘 중 하나 이상 설정 (LLM_PROVIDER 미지정 시 존재하는 키로 자동 선택, Anthropic 우선)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# 선택: "anthropic" | "openai" 로 명시적 지정 (미지정 시 자동 선택)
LLM_PROVIDER=

# 선택: 모델명 override
LLM_MODEL=

# 한 요청당 ClinicalTrials.gov에서 가져올 study 수 상한
MAX_STUDIES=500
